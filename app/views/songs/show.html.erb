<h2><%= @song.title %><%= @song.artist %></h2>
<h3> Associations: </h3>
<ul>
  <% @song.tapes.each do |tape| %>
    <% if tape.persisted? %>
        <li> <%= tape.name%> Destroy Association</li>
    <% end %>
  <% end %>
</ul>
