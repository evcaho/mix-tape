<h1> Mixtapes! </h1>

<table>
  <tr>
    <th>Name</th>
    <th>Songs</th>
  </tr>

  <% @tapes.each do |tape| %>
    <tr>
      <td><%= tape.name %></td>
      <td>
      	  <% tape.songs.each do |song| %>
            <ul>
              <li> <%= song.title %>, <%= song.artist %> </li>
            </ul>
          <% end %>
      </td>
      <td><%= link_to 'Edit', tape %></td>
      <td><%= link_to 'Destroy', tape, method: :delete, data: { confirm: 'Are you sure?' } %> </td>
  	</tr>
<% end %>
</table>



